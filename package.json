{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/react": "^18.0.26",
    "lerna": "^6.1.0",
    "prettier": "^2.8.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start:main": "lerna run start --scope=@mf/main",
    "start:media": "lerna run start --scope=@mf/media",
    "start:all": "lerna run start --parallel --scope=@mf/main --scope=@mf/media",
    "build": "lerna run build"
  }
}
