{
  "name": "@mf/profile",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.19.3",
    "cross-env": "^7.0.3",
    "eslint-config-react-app": "^7.0.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "typescript": "^4.9.3"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "build": "yarn run clean && yarn run build:types && yarn run build:js",
    "build:js": "cross-env BABEL_ENV=production && babel src -d dist --extensions .ts,.tsx",
    "build:types": "tsc"
  }
}
